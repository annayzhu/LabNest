# Issue tracker

GitHub repository: annayzhu/LabNest.
Use the configured GitHub connector or authenticated GitHub CLI. Implementation specs use the existing `ready-for-agent` label. Current independent procurement/inventory issue: #68.

Do not treat issue publication or a merged PR as functional acceptance. Keep completed, failed and unexecuted checks separate. Merge, deployment and production-data actions require user authorization.
