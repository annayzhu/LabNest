# 2026-09-15 计算器补充验收

基线：main `344014a24b8b58c1bfd87378157298e6d7d9162d`（PR #78）。本次仅实施附件第7.1节及A29-A33；此前验收矩阵保持有效，未重做计算、单位、缓存或实验记录系统。

应用提交：`ff7ed43`、`35abfcb`。PR #79 的最终 CI 与正式部署版本在交付记录另外核实。

| 要求 | 结果 | 修改及证据 |
|---|---|---|
| A29 单层离线管理 | 通过 | CalculatorMore页头菜单直接打开dialog；正文无离线折叠，面板内无details；browser.json及offline截图 |
| A30 预设紧随计算 | 通过 | CalculatorPresets在Calculate之后、手机结果入口之前；空态仅保存，少量select，较多可搜索；桌面/手机浅深色截图 |
| A31 预设完整恢复 | 通过 | 复用addPreset/restoreLegacyInputs，新增仅改名称的renamePreset；确认载入/取消，重命名与刷新，搜索回车不提交计算，删除确认；单位换算2mg→2000µg跨维度恢复；单位测试及browser.json |
| A32 离线状态及更新 | 通过（localhost） | 实际worker准备主页/当前页，失败后Retry，成功后Update，再更新、真实浏览器断网刷新、恢复草稿和计算；offline.json。不声称所有工具均离线 |
| A33 清理保护 | 通过 | 真实clear后逐字比较localStorage草稿、非空合成预设/历史及IndexedDB非空合成blocked队列；clear-failure.json注入负回复，缓存保留、重试后删除 |

两个独立审查均完成。审查发现的跨维度预设丢单位、搜索回车误提交、字号及焦点风格问题已修正并复核。测试边界是已授权的预设保存/恢复与离线管理行为，不测试静态间距的实现镜像。

完整本地套件109文件、630测试通过；TypeScript及生产构建通过。Lint无错误，保留10项既有警告。最终CI另外覆盖双时区及原22组浏览器验收，不用测试数量替代上表的页面证据。

## 失败与未执行

最终已执行项目无未解决失败。初轮故障注入使用page路由，无法拦截worker网络请求，改为context路由后实际失败/重试通过；搜索测试初轮将Next空live-region误当错误，已改为校验非空错误及input.form为null。

真机软键盘/中文输入法、真实手机160%及电脑200%缩放、实验人员试用、原故障记录及物理打印仍未执行。手机局域网HTTP不满足受信任HTTPS离线条件，localhost断网通过不能代表手机离线通过。

手机可复验：打开计算器，载入示例，点击计算下方“存为预设”，命名保存；修改参数后选择预设，分别取消和确认；更多→离线使用应只显示一层管理面板。真实缩放160%后检查长名称、搜索和按钮可操作性。
