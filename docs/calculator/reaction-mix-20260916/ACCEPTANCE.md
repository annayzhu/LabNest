# 2026-09-16 反应配液验收

基线 main：`0402a9a3aef554ff74b274853febd5546e186878`。实现版本：`44c5c85804e849b9fbdfe18f9849712885f0ef5e`。后续交付提交仅增加验收资料；合并及运行版本另见部署记录。

使用隔离数据库和合成记录，未访问或改写用户实验数据。浏览器运行生产构建，1440 / 700 / 390 CSS px，浅色及深色。手机宽度为 Chromium 模拟，不等于真机。

| 附件要求 | 结果 | 修改位置及证据 |
|---|---|---|
| 预设、示例、草稿在输入区且早于计算 | 通过 | CalculatorWorkspace；[浏览器记录](evidence/browser.json)、input-* 截图 |
| 添加、删除和分组靠近输入；复制、导出在结果区 | 通过 | StructuredInputs、ReactionMixResult；input-* / result-* 截图 |
| 恢复完整输入，载入使旧结果失效 | 通过 | 首次草稿快照；先载示例再恢复 G_mix，预设载入后结果清空，browser.json |
| 手机宽度和桌面缩窄后，成功切结果且顶部可见 | 通过 | 实际标签布局检测；390 / 700 和 1440→700 页面操作 |
| 输入错误留在输入并定位且保留值 | 通过 | reaction-input-focus；零总体积、负组计数、重复组名、负组分及未选中组的不可行浓度 |
| 宽屏并排，载入、恢复、调整窗口不抢切 | 通过 | 1440宽屏及1440→700；browser.json |
| 返回输入修改再计算，结果更新 | 通过 | 返回后改总体积0使结果失效，改10重新计算；显示单位mL时原输入仍为9µL |
| 单组真实复制至文本框符合样例 | 通过 | 通过系统剪贴板粘贴至另一页面文本框，精确匹配，[实际文本](evidence/single-390-light.txt) |
| 多组、跨组同名组分、单独加入清楚区分 | 通过 | [全部组文本](evidence/multi.txt)、[步骤](evidence/multi-steps.txt)；非预混不增加余量 |
| 整批237.6µL，分装9µL×24 | 通过 | 公共resultClipboard精确数值测试与6种页面组合 |
| 选择配液组位于结果区且含义明确 | 通过 | 实际无“加载配液组”；结果使用“选择配液组”，不修改输入 |
| 单位在表格上方且不改变输入 | 通过 | µL→mL，132→0.132，原reactionVolumeUl仍9；复制与CSV数值回归 |
| 合并独立配液与完整结果 | 通过 | 单一分组表，浏览器断言仅一张结果表 |
| 删除冗余说明，保留错误和必要依据 | 通过 | master-mix不显示通用方法区；警告直接显示，实际余量/分装依据可展开 |
| 复制组名/分装摘要先于组分 | 通过 | 单组精确文本及多组文本 |
| 六步顺序、单语言、加样对象 | 通过 | [六步文本](evidence/steps-390-light.txt)；有合成孔位上下文显示孔加样，否则显示个反应 |
| 多组步骤与复制全部不遗漏隐藏组 | 通过 | G_one/G_two切换、复制全部/当前组，失败重试仍为当前组文本 |

## 视觉复核

完成一轮批量检查和一轮修正后确认。手机结果表四列无需横向滚动；展开区有明确箭头；输入工具按行紧凑排列。保留输入组分表内部滚动以访问所有编辑列，页面本身无横向溢出。所有截图为真实页面，未制作设计稿替代。

[桌面输入浅色](evidence/input-1440-light.png) · [桌面结果深色](evidence/result-1440-dark.png) · [手机输入深色](evidence/input-390-dark.png) · [手机结果浅色](evidence/result-390-light.png) · [窄窗口结果](evidence/result-700-light.png)

## 独立审查与已修正项

- Spec审查：复制重试事件参数/当前组范围、未选中组错误定位已修复；不可行浓度换算后的目标字段已补页面回归。
- 通用代码审查：复制当前组重试、所有组分单独加入时遗漏反应数已修复并回归。
- 没有更换计算算法、存储结构、单位系统或历史快照；standalone bundle随公共复制展示逻辑同步生成。

## 未执行及边界

- 真机软键盘、真实160%缩放、实验人员试用：未执行。本次模拟手机不能替代这些项目。
- 原故障历史记录：未回读，本次使用合成记录。
- 无法将“未执行”视为通过；上述事项不阻断其余已授权开发与部署。真机可打开交付地址，载入自己的预设，编辑中文组名和数值，计算后核对切换/顶部，缩放160%，返回修改，再复制到备忘录逐项核对。

## 自动验证

生产构建、TypeScript、110文件634项测试通过；lint为0错误、10条既有警告。完整25组浏览器回归全部通过（含31工具页面/正式导出、Run、离线同步、数据库回读、移液、旧记录、预设及本轮用例），见 [汇总日志](evidence/logs/regression.log) 和 [分项结果](evidence/regression.json)。计算器验收当前无失败项。完整历史回归输出保留在本地部署备份的 final-local-regression；CI双时区及编辑器回归链接见PR。

编辑器CI初次运行在图片回退时出现`decode()`节点替换竞态（run 35089756710）；测试改为先等待实际原图URL、complete及naturalWidth，再验证decode。未修改产品图片代码。隔离数据库的定向图片回退/双源失败/恢复重试用例已通过，见 [记录](evidence/editor-recovery.json)。最终CI状态以PR最新提交为准。

计算器CI run 35090287642的前24组既有回归均通过，新增用例过早检查requestAnimationFrame中的字段focus而失败。新增用例现等待指定字段实际取得焦点，仍保留焦点精确断言；移至总回归首位以更早发现问题。产品代码无变化。
