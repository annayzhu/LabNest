# 反应配液调整

基线 main: `0402a9a3aef554ff74b274853febd5546e186878`。原主工作目录有无关修改，本次在独立工作树实施。

附件已完整核对，第1节替代9月15日的预设位置要求。本次属于现有操作工具细化，沿用主题、字体及控件体系，不采用营销页布局。视觉变化3、动效1、信息密度7：输入准备→参数/组分→计算→配液清单→复制/导出。

## 实际核查与处理

- 预设、示例、草稿原来在计算之后，移入反应配液输入标题工具区；其他工具位置保留。
- 初始草稿首次读取后保留快照，避免载入示例或修改覆盖“上次草稿”的恢复目标。首次空页不保存空白。
- 切单组原来显示旧隐藏rows，改为确认后保留第一组的真实组分及反应数；组内编辑仅影响该组。全局总体积和余量在附近帮助说明。
- 未找到“加载配液组”按钮。实际存在输入组导航和结果组导航；结果选择明示“选择配液组”。
- 反应配液合并原汇总、组表与“完整结果”重复表为单一配液清单；去掉泛化“结果说明”和“方法与假设”，提供含当前值的计算依据。
- 表格单位移到表头工具区，统一控制表格、配液清单复制和步骤体积，输入量不变。导出放在结果标题，仍保留完整结构化追溯数据。
- 操作步骤从原operations顺序分组显示：配制、加入预混组分、分装、单独加样。不重新计算剂量。
- 仅反应配液显式计算成功且实际标签布局可见时切结果；错误留输入并定位，载入与恢复不触发跳转。

## 验证边界

使用附件独立算例对公共 resultClipboard 接口做精确文本断言；页面检查真实剪贴板粘贴、预设与草稿往返、窄屏成功/失败、单位与分组；现有计算和导出回归保留。记录为合成验收数据。真机键盘、真实缩放与人员试用若未实际执行，不计为通过。
