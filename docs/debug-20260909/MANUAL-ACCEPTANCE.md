# 尚需真实环境确认

这些项目未执行，不能用 Playwright 手机尺寸或合成数据替代。自动验收全部使用隔离测试数据库；测试页不是正式部署。

1. **真机中文输入与软键盘**：在耗材名称、图片图注、Run偏差文字中输入“培养基更换”，用拼音选词并回车确认；应只确认候选词，不意外提交或关闭。关闭后重开应保留草稿，保存刷新后仍显示中文。
2. **真机160%缩放**：手机系统/浏览器缩放设为160%，打开Run、耗材弹窗、实验简介、属性面板及四工具。检查完整文字、数字与单位同排、按钮可点、无页面横向滚动；记录设备、系统、浏览器和缩放方式。不要用CSS zoom截图代替。
3. **真实打印和驱动覆盖**：六类文档及模板各取一份，确认默认A4；实际打印一页含表格和图片的文档，再检查驱动选择其他纸张时的行为。自动检查的14份PDF/16页为Chromium输出，不代表实体打印机结果。
4. **原故障记录**：提供或选择原来出问题的记录ID，按只读查看→重新编辑→另存PDF核对图片、历史来源及异常文字。未经这一步，不宣称所有历史记录恢复。不要用缺失字段推造原因或处理结论。
5. **实验人员试用**：选一项真实工作流程，由使用者完成规程选择、Run偏差记录、耗材预计/实际确认、返回实验正文及打印，记录理解不清或操作阻碍。

影响：真机触控、IME、缩放和实体打印仍不具备完整验收证据；原故障记录的恢复范围未确认。该限制不影响已经逐项验证的合成记录保存、库存幂等、离线同步及PDF页面尺寸结果，也不能将前者自动记为通过。
