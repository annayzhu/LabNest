# 未执行的真机与人员验收

这些项目未执行，不计为通过。它们影响真机交互/物理输出的最终验收，不否定已注明范围的自动化结果。

1. 手机连接正式服务所在网络，进入一个可更改的测试Run；查看含宽表步骤，向右滑到最后一列、向下到最后一行。确认不误切步骤。
2. 打开“记录”或偏差输入，用中文拼音选词并按回车确认；检查只完成选词、不提交表单。键盘打开时仍可看见输入与保存操作。
3. 将真机显示/浏览器缩放调至160%；检查工具名称、单位、表格和确认按钮，不裁切或整页横向滚动。完成后恢复常用缩放。
4. 填写未保存偏差和测量，关闭重开，再往返计算器；核对步骤、输入和计时状态。只提交一份测试记录，刷新确认无重复。
5. 用真实打印机以A4打印含图片、宽表与偏差的测试文档；检查每页正文、图注、边界、末行及标签。PDF检查不能替代此步。
6. 请一位实验人员从步骤开始到记录、计算、返回和确认完整操作一次，记录找不到入口或误操作之处。

原用户故障记录尚未明确对应；不声明所有历史记录恢复。手机HTTP环境不能视为完整可信HTTPS离线环境，证书/网络条件仍须单列。
