# 双轴审查

基线 `4f93dc8910cdc2b9f55914e632a82154f470caa8`。初始审查代码 f390873；该提交因新增 workflow 权限被正常推送拒绝，移除 workflow 后本地重记为89c6d74。未部署初始提交。

## Standards
- 引用内部段落对齐/段距丢失：已修复，新增公开转换往返用例。
- 测量保存后保留已提交值/旧时间导致再次提交风险：成功保存或入队后明确结束草稿；失败及未提交继续保留。独立代码复核通过，页面及数据库数量验收另列。

## Spec
- 旧Protocol heading/text/Checklist的格式：在原块类型/id上保存rich nodes，未改变source_ref或确认数量。
- 嵌套普通列表、编号项与Checklist后续说明：保留原生子内容；编号项的子内容进入同一个Run步骤，子Checklist不额外升级为执行步骤。
- N05初审误判已撤回：实验编辑通过trailingContent共享只读派生渲染，原手写document独立编辑。
- 偏差收纳增加类型和简短摘要；H2/H3/Body及间距角色接入共享消费者。

修复代码 `231fd34`。审查不是页面验收；实际结果见总清单和最终构建日志。

## 实际页面追加发现

- 四种表格对齐：单段落、无内联标记的单元格未触发富内容保存。新增独立单段落四对齐用例；多段落另测换行，避免一个条件掩盖另一个。
- 正文保存误清步骤完成状态：移除实验编辑动作对 Run 勾选的写入。真实页面红例与 action 回归均捕获原故障；不通过补隐藏旧值回放来掩盖并发风险。
- 嵌套结果模板的独立校验 schema 丢弃段落布局：与既有 Protocol 适配器共用 paragraphLayoutFields，并保留子列表内容和字体。没有新建模板或编辑系统。
- 中文草稿提示去除双语重复；工具栏只有真正按钮获得 menuitem 角色。
- 窄属性头部使用两列，避免保存按钮因宽度落到独立行；折叠内容不再每次重播入场动画。

Inventory workflow 曾通过正常Git推送尝试，被凭据缺少 workflow scope 拒绝。已移除本轮workflow写入，不通过其他接口绕过权限；独立CI仍未启用。

## 轻量编辑器粘贴追加审查

真实剪贴板用例复现 Entry 将表格压成文字。复用既有 TableKit / Scientific table schema 与只读表格组件，扩展原 Markdown 字段的表格封套；未新建文档、编辑器或存储系统。模板沿用已有 rich-node childContent。

独立审查发现：新封套内图片最初没有进入附件遍历，已用独立解码器打通共用媒体检查，新增正式附件、有效UUID的pending上传和data临时地址回归。rows与cellRichContent是同图的两种持久化表达，附件关联原本按ID去重；测试明确核对唯一ID，不误要求媒体遍历只有一个实例。实际图片关联回读见最终浏览器/数据库证据。
