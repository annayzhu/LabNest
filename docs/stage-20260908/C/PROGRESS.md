# C 工作线阶段记录（未交付）

基线：1aed227f34e0dbe3558fbb30fcd503aaaef3dd4a。当前为未提交工作区，不是发布版本。

图片保存和打印空白依用户核查调整为：修复已合并，保留回归验收，核对原故障记录。用户提供的 14 份 PDF、18 页及独立 26 项测试不计为本轮新增测试。原故障记录和真实打印机尚未执行。

## 本轮局部通过

- 实验正文页面：全文 18pt 覆盖正文内标题、章节标题、列表、图注和跨章节表格；保存、刷新只读后字号保持。
- 单格 12pt、整行 14pt、整表 20pt 不改变表内数值，其他范围正文字号不变。
- TypeScript 检查通过；Scientific/Protocol Tiptap 兼容回归共 10 项通过（仅上海时区）。命令中指定的 protocol-docx-export.test.ts 文件并不存在，不能算 DOCX 验收。

复现：在仅含合成记录的 labnest_editor66_acceptance 数据库环境运行 `node --import tsx scripts/seed-stage-text.ts`，使用端口 3231 的本分支开发服务，再运行 `node scripts/verify-stage-text.mjs`。每次重新播种独立合成记录，避免前一次格式修改影响初态。浏览器验证直接操作表单、保存并刷新，不依赖内部编辑器对象。

## 尚未执行或未完成

多格与混合字号、嵌套说明全文范围、其他编辑入口、移动端与真机、右侧属性面板、正式导出逐文件检查、生产构建及第二时区回归。没有新 PR，未合并或部署。不应将本阶段记录用于关闭 T27/T28/T29 或四条工作线。
