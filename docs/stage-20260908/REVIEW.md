# 只读复核与修复

按 code-review 技能两路只读复核。发现并修复：

1. 原单位bottle/box被Calculator未注册单位转换清空：同单位不转换；5bottle页面保存/回读通过。
2. 未知库存被“未指定”锁死：首次盘点可建立新单位；180mL保存回读通过。
3. Run仅体积单位：空值可明确选质量/包装/自定义；5mg保存刷新/API回读通过。
4. 嵌套说明“全文”仅改局部：根documentEditor与局部editor分开；先全文12pt，再从说明全文18pt，外层24px，保存/DOCX通过。
5. 既有账本清空后可选不兼容单位：存储单位锁定时菜单仅同维；生产页面验证清空后无mg选项。

保留跨维度换算限制，不猜测材料参数。最后一项是对复核意见的追加修复，应用20e8855；全部后续合并应用目录不变。未发现其他确定阻断不等于穷尽性证明。真机项继续未执行。
