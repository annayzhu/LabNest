import assert from 'node:assert/strict';
import {writeFile} from 'node:fs/promises';
import {chromium} from 'playwright';
const base=process.env.LABNEST_E2E_BASE_URL??'http://localhost:3221';
const browser=await chromium.launch();const context=await browser.newContext({viewport:{width:1280,height:900}});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));
try{
 await page.goto(`${base}/tools/calculator/dilution?experimentId=calculator-acceptance-experiment&experimentStepId=calculator-acceptance-step`,{waitUntil:'networkidle'});
 for(const [name,value] of [['Stock concentration','10'],['Target concentration','10'],['Final / initial volume','2'],['Operator','Acceptance fixture operator']])await page.getByRole('textbox',{name:new RegExp(`^${name}`)}).fill(value);
 await page.getByText('Pipetting equipment and rounding (optional)',{exact:true}).click();await page.getByRole('textbox',{name:'Equipment minimum µL',exact:true}).fill('3');await page.getByRole('combobox',{name:'Take stock display unit',exact:true}).last().selectOption('nL');
 await context.setOffline(true);await page.getByRole('button',{name:'Record in experiment',exact:true}).click();await page.getByText('Added to sync queue; check sync status for completion.').waitFor();
 const queued=await page.evaluate(async()=>{const db=await new Promise((resolve,reject)=>{const r=indexedDB.open('labnest-mobile-mutations',1);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});return new Promise((resolve,reject)=>{const r=db.transaction('mutations').objectStore('mutations').getAll();r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});});assert.equal(queued.length,1);
 const mutation=queued[0];assert.equal(mutation.payload.snapshot.displayUnits.stockVolumeUl,'nL');assert(mutation.payload.snapshot.structuredWarnings.length>0);const body={...mutation.payload,clientMutationId:mutation.clientMutationId,deviceCreatedAt:mutation.deviceCreatedAt};
 const synced=page.waitForResponse(response=>response.url().endsWith('/api/mobile/calculations')&&response.request().method()==='POST');await context.setOffline(false);const response=await synced;assert.equal(response.status(),201,await response.text());const result=await response.json();
 const replay=await context.request.post(`${base}/api/mobile/calculations`,{data:body});assert.equal(replay.status(),200);assert.equal((await replay.json()).resultId,result.resultId);
 const wrong=await context.request.post(`${base}/api/mobile/calculations`,{data:{...body,experimentStepId:'missing-step'}});assert.equal(wrong.status(),409);
 const archived=await context.request.post(`${base}/api/mobile/calculations`,{data:{...body,experimentId:'calculator-archived-experiment',experimentStepId:'calculator-archived-step'}});assert.equal(archived.status(),409);
 const foreign=await context.request.post(`${base}/api/mobile/calculations`,{data:body,headers:{Origin:'https://untrusted.invalid'}});assert.equal(foreign.status(),403);
 const forged=await context.request.post(`${base}/api/mobile/calculations`,{data:{...body,snapshot:{...body.snapshot,outputs:[]}}});assert.equal(forged.status(),409);
 const stripped=await context.request.post(`${base}/api/mobile/calculations`,{data:{...body,clientMutationId:crypto.randomUUID(),snapshot:{...body.snapshot,warnings:[],structuredWarnings:[]}}});assert.equal(stripped.status(),409);
 await page.goto(`${base}/results/${result.resultId}`,{waitUntil:'networkidle'});await page.getByRole('heading',{name:'计算快照 / Calculation snapshot'}).waitFor();
 await page.goto(`${base}/experiments/calculator-acceptance-experiment/run`,{waitUntil:'networkidle'});await page.getByRole('button',{name:'计算 / Calculate',exact:true}).click();const dialog=page.getByRole('dialog',{name:'Step calculations'});await dialog.waitFor();await page.keyboard.press('Escape');assert.equal(await dialog.count(),0);assert(await page.getByRole('button',{name:'计算 / Calculate',exact:true}).evaluate(el=>el===document.activeElement));
 assert.deepEqual(errors,[]);const report={completedAt:new Date().toISOString(),resultId:result.resultId,clientMutationId:mutation.clientMutationId,checks:['Offline queue retained intent, selected nL display and structured pipetting warning','Reconnect created Result','Replay returned identical Result ID','Missing step rejected','Archived experiment rejected','Foreign origin rejected','Forged snapshot rejected','Fresh mutation with stripped warnings rejected','Result page renders immutable calculation snapshot','Desktop drawer Escape and focus restoration'],errors};await writeFile('docs/calculator/evidence/integration-report.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));
}finally{await browser.close();}
